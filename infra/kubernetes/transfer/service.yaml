apiVersion: v1
kind: Service
metadata:
  labels:
    app: account-service
  name: account-service
spec:
  ports:
  - name: 80-80
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: account-service
  type: ClusterIP
status:
  loadBalancer: {}
